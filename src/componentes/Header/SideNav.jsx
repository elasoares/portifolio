import { TfiAlignJustify } from "react-icons/tfi";

export function SideNav({ className, onClick }) {
  return (
    <>
      <TfiAlignJustify className={className} onClick={onClick} />
    </>
  );
}
